{% extends 'base.html.twig' %}

{% block title %}Редактировать заявку{% endblock %}

{% block body %}
	<div class="container">
		<div class="row">
			<div class="d-inline col"><a href="{{ path('mr_lead_index') }}" class="btn btn-primary btn-sm">Вернуться к списку</a></div>
			<div class="d-inline col">{{ include('mr_lead/_delete_form.html.twig') }}</div>
		</div>


		<div class="card mt-3">
			<div class="row col-md-12">
				<h1>Редактировать заявку</h1>
			</div>
			<div class="row col-md-12">
				<div>{{ include('mr_lead/_form.html.twig', {'button_label': 'Сохранить'}) }}</div>

			</div>
		</div>
	</div>
{% endblock %}
