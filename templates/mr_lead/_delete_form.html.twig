<form method="post" action="{{ path('mr_lead_delete', {'lead_id': mr_lead.id}) }}" onsubmit="return confirm('Будет удалена заявка?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mr_lead.id) }}">
    <button class="btn btn-danger btn-sm">Удалить заявку</button>
</form>
