{% extends 'base.html.twig' %}

{% block title %}Редактирование книги{% endblock %}

{% block body %}
	<div class="container">
		<a href="{{ path('book.list') }}" class="btn btn-primary btn-sm">Вернуться в список книг</a>
		<h1>{{ mr_book.getName }}</h1>
		{{ include('mr_book/_form.html.twig', {'button_label': 'Сохранить'}) }}
	</div>
{% endblock %}
